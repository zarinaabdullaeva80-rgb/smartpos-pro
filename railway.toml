[build]
builder = "dockerfile"
dockerfilePath = "server/Dockerfile"
watchPatterns = ["server/**", "mpos/dist/**"]

[deploy]
startCommand = "node src/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/api/health"
healthcheckTimeout = 10
